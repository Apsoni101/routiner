import 'package:flutter/material.dart';

class AchievementsTabView extends StatelessWidget {
  const AchievementsTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('achievements Content'),
    );
  }
}
